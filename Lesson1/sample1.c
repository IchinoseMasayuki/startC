#include <stdio.h>			/*stdio.hというファイルを#で最優先に読み込む*/
					/*stdio.h等をヘッダーファイル(header file)という*/
int main(void)				/*Integer(整数)	4bitの整数を格納することができる。*/
{					/*main(主要部)	ここから本体である。*/
					/*void		ここではmain関数に置ける変数がないこと。*/
	printf("ようこそC言語へ\n");	/*printf	()内の""内を表示する。*/
					/*\n		改行*/
	
	return 0;			/*return	値を返す。*/
}
